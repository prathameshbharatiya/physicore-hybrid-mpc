{
  "entities": {
    "AllowedUser": {
      "title": "Allowed User",
      "description": "A user authorized to access the simulator.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the allowed user."
        },
        "hasTested": {
          "type": "boolean",
          "description": "Whether the user has already used their one-time test."
        },
        "role": {
          "type": "string",
          "enum": ["admin", "client"],
          "description": "The role of the user."
        }
      },
      "required": ["email", "hasTested", "role"]
    }
  },
  "firestore": {
    "/allowed_users/{userId}": {
      "schema": "AllowedUser",
      "description": "Collection of users allowed to access the application."
    }
  }
}
